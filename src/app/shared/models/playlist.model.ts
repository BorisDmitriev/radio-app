export interface Playlist {
  playlist_id: string;   
  name: string;
  description?: string;
  created_at: string;   
}

export interface PlaylistView {
  playlist_id: string;   
  name: string;
  description?: string;
  created_at: string; 
}